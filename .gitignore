# Private keys — never commit
root/etc/ssl/private/
*.key

# Byte-compiled / optimized / DLL files
**/__pycache__/
*.py[cod]
*$py.class

# C extensions
*.so

# Distribution / packaging
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
share/python-wheels/
*.egg-info/
.installed.cfg
*.egg
*.swp
*.pyc

# PyInstaller
# Usually these files are written by a python script from a template
# before PyInstaller builds the exe
.venv
SPIE/

# OpenSSL CA serial state, written by `make make-certs` (-CAcreateserial).
# Local bookkeeping, regenerated on demand; not part of the deployed tree.
*.srl
